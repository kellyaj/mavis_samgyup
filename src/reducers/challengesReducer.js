import {
} from '../actions/ActionTypes'

const challengesReducer = (challenges = {}, action) => {
  switch(action.type) {
    default:
      return challenges
  }
}

export default challengesReducer
